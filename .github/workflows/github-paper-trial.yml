name: Retired GitHub PAPER Trial V2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy PAPER V2 is historical evidence only
        run: |
          set -euo pipefail
          echo "RETIRED: PAPER V2 no longer runs on push, cron, or issue comments."
          echo "Canonical scheduled PAPER workflow: github-paper-v5.yml (R4.5)."
          echo "Existing V2 releases/artifacts remain immutable historical provenance."
          echo "This workflow does not create state, execute PAPER, publish Cloudflare, or use write permissions."

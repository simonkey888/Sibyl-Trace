name: Retired Cloudflare Terminal Legacy Publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publisher is permanently read-only
        run: |
          set -euo pipefail
          echo "RETIRED: this workflow cannot deploy sibyl-trace."
          echo "Canonical public publisher: publish-cloudflare-terminal-v5.yml"
          echo "Historical PAPER V2 + Research V3 evidence remains in GitHub artifacts only."
          echo "No Cloudflare credentials are loaded and no wrangler deploy command exists here."
